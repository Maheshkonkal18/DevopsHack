name: CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ harshavardhan234 }}
          password: ${{ Mahesh@18 }}

      - name: Build & Push Backend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/lostfound-backend:v1 ./server
          docker push ${{ secrets.DOCKER_USERNAME }}/lostfound-backend:v1

      - name: Build & Push Frontend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/lostfound-frontend:v1 ./client
          docker push ${{ secrets.DOCKER_USERNAME }}/lostfound-frontend:v1
