apiVersion: v1
kind: Secret
metadata:
  name: lostfound-secrets
  namespace: lostfound
type: Opaque
stringData:
  MONGO_URI: "your-mongodb-connection-string-here"
  JWT_SECRET: "some-strong-secret-key"
  # add more if your backend .env needs them
